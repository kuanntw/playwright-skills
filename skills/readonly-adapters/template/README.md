# Template Adapter

Copy this folder to `skills/readonly-adapters/<project-name>` and customize:
- `baseURL`
- `routes`
- `selectors`

Optional: add login helpers and data bootstrap readers that still obey read-only rules.
